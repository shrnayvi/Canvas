var WINDOW_WIDTH = window.innerWidth;
var WINDOW_HEIGHT = window.innerHeight;
var GRAVITY = 1;
var FRICTION = 0.1;
var JUMP_HEIGHT = 30;
var SURFACE_HEIGHT = 200;
var PI = Math.PI;
var TWO_PI = PI * 2;
